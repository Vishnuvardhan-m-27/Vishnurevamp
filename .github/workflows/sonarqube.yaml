name: SonarQube pipeline

on:
  # pull_request:
  #   branches: ["main"]
  workflow_dispatch:

jobs:
  sonarqube:
    runs-on: [self-hosted, wsl-vishnu]

    steps:
      - name: Clone code
        uses: actions/checkout@v7

      - name: Sonarqube Scan
        uses: SonarSource/sonarqube-scan-action@v8
        with:
          args: >
            -Dsonar.projectKey=vishnu
            -Dsonar.sources=.
            -Dsonar.host.url=http://localhost:9000 
            -Dsonar.login=sqp_ea0be4ea99ccf66ae505969181ef7ead1506c29d
